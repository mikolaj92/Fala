# Optional examples (not Fala core)

- `domain-packs/` — Splot/Signals vocabulary mappings; domain-specific, not the organ.
- `runtime/` — historical Python demos.

Core product examples: `examples/correlation-paths/basic/` (native TOML + effectors as OS children).

