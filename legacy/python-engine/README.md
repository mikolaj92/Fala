# Legacy CPython Fala engine (not product)

**Product runtime is Mojo** (`mojo/fala`). This tree is the retired CPython
implementation kept only as a historical reference / oracle.

- Do not add features here.
- Do not treat `python_function` or Python `subprocess` as Fala identity.
- Safe to delete after Mojo land is complete.

Install (historical only): see `legacy/python-engine/pyproject.snippet` or wire
locally; the root `pyproject.toml` no longer packages this engine.

